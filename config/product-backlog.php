<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Still to build
    |--------------------------------------------------------------------------
    |
    | Features that belong on the matches calendar but are not built yet.
    | Staff see this list on /calendar. Guests, shooters and match
    | directors do not. Add the next item here.
    |
    | `audience` is who the feature is for once it ships. `pro` is the
    | paid plan. Omit it when the feature is for every shooter.
    |
    */

    'items' => [
        [
            'title' => 'Firearm licence renewal reminders',
            'audience' => 'pro',
        ],
    ],

];
